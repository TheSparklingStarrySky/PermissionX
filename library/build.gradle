apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'maven-publish'
apply plugin: 'signing'
android {
    compileSdkVersion 30
    buildToolsVersion "30.0.3"


    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 30

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles 'consumer-rules.pro'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
    implementation 'androidx.appcompat:appcompat:1.2.0'
    implementation 'androidx.core:core-ktx:1.3.2'
    testImplementation 'junit:junit:4.12'
    androidTestImplementation 'androidx.test.ext:junit:1.1.2'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.3.0'
}

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.vanniktech:gradle-maven-publish-plugin:0.17.0'
    }
}

allprojects {
    plugins.withId("com.vanniktech.maven.publish") {
        mavenPublish {
            sonatypeHost = "S01"
        }
    }
}

apply plugin: "com.vanniktech.maven.publish"
/*
publishing {
    publications {
        maven(MavenPublication) {
            groupId 'io.github.TheSparklingStarrySky' //开通maven central时候定义的
            artifactId 'permissionx' //资源名称
            version '1.0.0' //版本名称
            pom {
                name = 'PermissionX'
                description = 'Make Android runtime permission request easy.'
                url = 'https://github.com/TheSparklingStarrySky/PermissionX'
                inceptionYear = '2021'
            }
            scm {
                url = 'https://github.com/TheSparklingStarrySky/PermissionX'
                connection = 'scm:git:git://github.com/TheSparklingStarrySky/PermissionX.git'
                developerConnection = 'scm:git@https://github.com/TheSparklingStarrySky/PermissionX.git'
            }

            licenses {
                license {
                    name = 'The Apache Software License, Version 2.0'
                    url = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    distribution = 'repo'
                    comments = 'A business-friendly OSS license'
                }
            }

            developers {
                developer {
                    id = 'TheSparklingStarrySky'
                    name = 'Bingbing Zhang'
                    email = '2386243281@qq.com'
                    url = 'https://github.com/TheSparklingStarrySky'
                }
            }

            issueManagement {
                system = 'Github'
                url = 'https://github.com/TheSparklingStarrySky/PermissionX'
            }
            artifact generateSourcesJar //需要上传的source jar
            artifact generateJavadoc //需要上传的java doc
            artifact makeJar //需要上传的资源jar路径或者是aar路径，这边可以填写一个生成jar的task，如makeJar方法就是一个生成jar的task
        }
    }
    repositories {
        maven {
            // 指定要上传的maven仓库
            url = 'https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/'
            //Maven仓库用户名和密码
            credentials {
                username mavenCentralUsername
                password mavenCentralPassword
            }
        }
    }
}
signing {
    sign publishing.publications.maven //签名配置
}*/
